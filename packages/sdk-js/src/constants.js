// constants.js

/**
 * Known deposit statuses.
 * These should match the backend implementation.
 */
export const DepositStatus = Object.freeze({
  PENDING: "pending",
  FORWARDED: "forwarded",
  FAILED: "failed"
});
