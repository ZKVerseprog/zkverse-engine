# @zkverse/engine-sdk

JavaScript SDK for interacting with the zkVerse Engine backend.

This SDK wraps the public HTTP API:

- `GET /health`
- `POST /api/deposits`
- `GET /api/deposits/:id`

It can be used in:

- browser frontends (dApps),
- Node.js backend services,
- bots and off-chain automation.
